import { movies } from '../types'
interface props{
    movie:movies
}
export default function MoviePreview(props:props) {
    const {movie} = props
  return (
    <div className=''>
        
    </div>
  )
}
